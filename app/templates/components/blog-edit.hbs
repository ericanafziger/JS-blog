<!-- Nested in manage-blog -->
<img src={{post.cover}}>
<div class="hover-text">
  {{#link-to 'edit' post.id}}
  <h2>{{post.title}}</h2>
  <h4>{{post.place}}</h4>
  <h3>{{post.type}}</h3>
  {{/link-to}}
</div>
